# Security

**Tier: CONTRACT** · Last verified: *(set when first populated)*

Authentication, sessions, authorisation, data handling, and the threats this design does and does not defend against.

> **New project:** this is a skeleton. Populate it before the work it governs begins, and set `Last verified`. An unpopulated CONTRACT file is a blocking task, not a licence to improvise.

## Authentication
## Sessions
## Authorisation model
Where it is enforced. Server-side enforcement is the only enforcement that counts — client-side checks are UX, not security.

## Data handling
What is sensitive, where it lives, who can read it, retention.

## Threat notes
What this design defends against, and what it explicitly does not.
